R is a very popular programming language for statistical computing and graphics.
These R scripts demonstrate the use of R Programming language for data science and analysis using R-studio.
Scripts start with the simplest queries, and gradually get to advanced ones too.

Download R language from https://cran.r-project.org/bin/macosx/Download R-studio from https://posit.co/download/rstudio-desktop/

ctrl + ↵ executes the line of code where the cursor is or the selected query (or queries).

Set the correct working directory at the start of all scripts.

Though the scripts are segregated, but some of them might require variables declared in previous scripts in sequence, so it is recommended to run the scripts in sequence and not clear the environment while proceeding to next script.